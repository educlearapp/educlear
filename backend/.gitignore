node_modules
dist

/src/generated/prisma

# Local secrets — never commit (Phase 6E)
.env
.env.local
.env.*.local

# Private Admissions uploads (OA-03D) — never public /uploads
data/admissions/

# Build-time copies of data/ support JSON for persistent disk repair (see stage-billing-disk-support-templates.mjs)
storage/billing-disk-support-templates/
